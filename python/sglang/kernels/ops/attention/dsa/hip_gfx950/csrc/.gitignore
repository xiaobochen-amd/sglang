# torch's hipify writes X.hip next to X.cu at build time and compiles THAT.
# Generated, never edited: keep it out of the tree. sync_kernels.sh deletes any
# stale copy, because a shadow left behind hides a freshly synced source.
*.hip
!logits_kernel.hip
!logits_bindings.hip
